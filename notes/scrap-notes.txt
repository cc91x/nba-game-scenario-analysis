How to run some go code:
go run test.go caller.go
    -> this will use logic from test.go and caller.go. I think there's a way to not specify the other calls from the cmd line

Very helpful getting started demo - https://www.youtube.com/watch?v=1MXIGYrMk80


How to not have if err == nil statements everywhere - https://go.dev/blog/errors-are-values
    - the trick is to refactor code to not be using it everywhere, some good patterns there

go-proj-game-sourcing.py, new game lookup file

https://code.visualstudio.com/docs/python/debugging - debug in python with argument, in the launch.json file here under .vscode folder


-- Script to create mongo db with team Ids 
nbaTeamIdCollection = mydb["nba-team-id-mapping"]
events = []
teamObjs = []
with open('/Users/ericwhitehead/Desktop/clag/nba-project/teamsToTeamId.csv', mode ='r') as file:
   first = True
   rows = csv.reader(file)

   for row in rows:
        if not first:
            events.append(row)
            teamObjs.append({
                'team-id': int(row[0]),
                'team-name': row[1],
                'team-abbreviation': row[2]
            })
            # teamInitToIdMap[row[2]] = row[0]
        first = False

nbaTeamIdCollection.insert_many(teamObjs)

go debug that seems to be working        {
            "name": "Go: Launch Program",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/clag/nba-project-post-mv",
            "env": {},
            "args": []
        },

NFl survivor pool - https://www.usafootballpools.com/nfl-pool-join-from-email-invitation?pool_id=DENPOOL&mid=500f9ca8badd713ae7b2ed2de17a4e28

